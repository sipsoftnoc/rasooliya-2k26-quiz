[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[dev]
  publish = "."
  functions = "netlify/functions"
[[redirects]]
  from = "/api"
  to = "/.netlify/functions/api"
  status = 200
